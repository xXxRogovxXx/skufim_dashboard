// Пароль для входа в дашборд.
//
// ⚠️ ВАЖНО: это ЛЁГКАЯ защита «от случайных глаз», а НЕ настоящая безопасность.
// Сайт статический и публичный (GitHub Pages), поэтому JSON-данные в
// web/public/data технически доступны напрямую по ссылке, минуя этот экран.
// Для настоящей защиты нужен приватный хостинг или прокси с авторизацией
// (например, Cloudflare Access).
//
// Как сменить пароль:
//   node -e "console.log(require('crypto').createHash('sha256').update('НОВЫЙ_ПАРОЛЬ','utf8').digest('hex'))"
// и вставьте полученный хэш ниже. Сам пароль в коде не хранится.
//
// Текущий пароль: skufim2026
export const ACCESS_PASSWORD_SHA256 =
  "b09850e00c17ed70dcfc45804057b7ec080c3eb717f0547d5304974c3a84ec9f";

// Ключ в localStorage, чтобы не спрашивать пароль каждый раз
export const ACCESS_STORAGE_KEY = "podcast_dashboard_access";
